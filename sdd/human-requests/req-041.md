# REQUISIÇÃO HUMANA REQ-041

* **Status**: `APPROVED`
* **Data de Entrada**: 2026-08-31
* **Autor**: Humano-no-Loop & Macro-Arquiteto
* **Lote Relacionado**: `BATCH-043`
* **Topologia de Agentes**: `triade`
* **Nível de Autonomia**: `supervisionado`
* **Repositórios Alvo**: `conn2flow-ai-workspace`

---

## Objetivo

Realizar o primeiro teste de validação fim-a-fim (End-to-End Probe) da **Tríade de Agentes Conectada via MCP Hub** (`conn2flow-hub`), verificando o ciclo completo de despacho (`dispatch_task`), execução com evidência (`report_completion`), auditoria técnica independente e homologação executiva da chefia.

---

## Escopo do Teste (Probe de Integração)

1. **Criação de Arquivo Teste/Sonda**:
   - O Agente Executor criará um arquivo de verificação de integridade `vscode-extension/test/mcpTriadProbe.test.cjs` validando que:
     * O manifesto de ferramentas do MCP Hub (`tasks/` e `completions/`) é reconhecido pela extensão;
     * Os diretórios de governança multi-repositório estão íntegros e com paridade.
2. **Execução e Registro no MCP Hub**:
   - O Executor roda `npm test` em `vscode-extension/` (garantindo que o novo teste passa junto com os 47 testes existentes).
   - O Executor registra a conclusão chamando a tool MCP `report_completion` (ou gravando o recibo em `completions/BATCH-043-receipt.json`).
3. **Revisão e Auditoria Técnica**:
   - O Revisor Técnico audita o diff, roda `npm test`, gera `sdd/validation/review-043.md` e emite o recibo de homologação.
4. **Fechamento do Arquiteto**:
   - O Macro-Arquiteto audita os recibos do MCP Hub e emite o relatório final ao Humano-no-Loop.

---

## Critérios de Aceite

- [x] Tarefa despachada via MCP Hub (`dispatch_task`) com registro em `tasks/REQ-041.json`.
- [x] Arquivo `vscode-extension/test/mcpTriadProbe.test.cjs` criado e passando na suíte de testes (`npm test`).
- [x] Recibo de conclusão emitido pelo Executor no MCP Hub (`report_completion`).
- [ ] Relatório de auditoria técnica `sdd/validation/review-043.md` emitido pelo Revisor com parecer `APPROVED`.
- [x] 100% dos testes automatizados passando (48/48 testes).
