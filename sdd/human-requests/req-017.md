# REQUISICAO HUMANA REQ-017 / BATCH-020

*   **Status**: `COMPLETED`
*   **Data de Criação**: 2026-08-24
*   **Data de Conclusão**: 2026-08-24
*   **Solicitante**: Chief Architect / Core Architecture Agent Memo
*   **Repositórios Alvo**: `conn2flow-ai-workspace`, `conn2flow`, `lumix`, `transformamp`, `conn2flow-site`
*   **Escopo**: Implementação da 33ª Skill (`c2f-tailwind-css-architecture`) nos moldes de IA em PT-BR e EN, resolução dos 4 pontos de atrito do Tailwind v4 (cascata, mascaramento por `css_compiled`, templates dinâmicos em runtime e build governado) e propagação universal.

---

## 🎯 1. Contexto & Problema Identificado

Nos últimos ciclos de desenvolvimento (Core, Lumix, Transform MP), foram identificados 4 pontos críticos de atrito com agentes de IA manipulando Tailwind CSS v4:
1. **Ordem de Cascata e Media Queries**: Classes isoladas com `.hidden` em páginas carregadas após o layout sobrescrevem classes `lg:flex` da sidebar no desktop.
2. **Mascaramento por `css_compiled`**: Páginas com CSS compilado prévio no banco de dados mascaram alterações feitas em arquivos físicos `.html` em `resources/`.
3. **Templates Dinâmicos em Runtime (Finding F2)**: Templates escolhidos via SELECT no banco de dados nascem fora do bundle em build-time e precisam de declaração em `tailwind_dependencies` nos metadados JSON.
4. **Tentativas de CLI Manual**: Agentes tentavam rodar `npx tailwindcss` solto, criando CSS fora da governança de `resources/`.

---

## 🛠️ 2. Especificação da Nova Skill: `c2f-tailwind-css-architecture`

Criada a skill `c2f-tailwind-css-architecture` em todos os 6 templates de kits de IA em `templates/pt-br/` e `templates/en/`:

### Estrutura Canônica em Português (`templates/pt-br/`):
```markdown
---
name: c2f-tailwind-css-architecture
description: "LEIA OBRIGATORIAMENTE antes de criar, alterar ou migrar qualquer tela, layout, página ou componente que utilize Tailwind CSS v4. Previne conflitos de cascata, mascaramento por css_compiled em banco, descarte de estilos em runtime e quebra de builds."
user-invocable: false
---

# Governança e Arquitetura do Tailwind CSS v4 no Conn2Flow e Projetos

# ⚡ Gatilho Obrigatório
- **TRIGGER**: Criar, alterar, migrar ou debugar qualquer layout, página, componente ou template com classes do Tailwind CSS v4.
- **SKIP APENAS SE**: Tarefas puramente de backend em PHP ou APIs sem renderização visual.
- **CONSEQUÊNCIA DE IGNORAR**: Quebra da cascata CSS (sidebar oculta no desktop), mascaramento de código pelo cache de banco (css_compiled) e descarte de estilos em runtime.

---

## ⛔ Regras Invioláveis:
1. **NUNCA execute comandos CLI manuais do Tailwind** (`npx tailwindcss`, etc.). Use sempre `./c2f resources:sync` (ou `php atualizacao-dados-recursos.php`).
2. **Todo HTML/CSS visual deve residir no Sistema de Recursos**: `resources/<idioma>/<tipo>/<id>/<id>.html` e `<id>.json`.
3. **Metadados JSON**: Todo recurso Tailwind DEVE declarar `"framework_css": "tailwindcss"` em seu arquivo de metadados `<id>.json`.
4. **Templates Dinâmicos em Runtime (Finding F2)**: Declare dependências de templates dinâmicos no array `"tailwind_dependencies": ["id-1", "id-2"]` do JSON do recurso pai.
5. **Limpeza Obrigatória de Cache no Banco (`css_compiled`)**: Ao editar arquivos físicos em disco, zere `paginas.css_compiled = NULL` no banco de dados para que o arquivo `.precompiled.css` entre em vigor sem mascaramento.
6. **Cascata e Media Queries**: Nunca use `.hidden` isolado em páginas filhas que conflitem com `lg:flex` ou `md:block` do layout pai; use sempre prefixos explícitos de breakpoint (ex: `hidden lg:flex`).
```

---

## 📋 Checklist de Execução

- [x] Criar a skill `c2f-tailwind-css-architecture/SKILL.md` em todos os 6 templates de kits em `templates/pt-br/`.
- [x] Criar a skill `c2f-tailwind-css-architecture/SKILL.md` em todos os 6 templates de kits em `templates/en/`.
- [x] Atualizar `docs/pt-br/CATALOGO-DE-SKILLS.md` e `docs/en/SKILLS-CATALOG.md` com a 33ª Skill (26 Core + 7 SDD Workflow).
- [x] Atualizar `conn2flow/cli/src/Commands/AiSyncCommand.php` para validar as 33 skills.
- [x] Executar `scripts/sync-all-repos.ps1` para propagar nos 4 repositórios alvo (`conn2flow`, `lumix`, `transformamp`, `conn2flow-site`).
- [x] Commitar e sincronizar no Git em todos os repositórios.
