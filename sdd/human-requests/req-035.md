# REQUISIÇÃO HUMANA REQ-035

* **Status**: `COMPLETED`
* **Data de Entrada**: 2026-08-29
* **Autor**: Humano-no-Loop
* **Lote Relacionado**: `BATCH-037`
* **Topologia de Agentes**: `triade`
* **Nível de Autonomia**: `supervisionado`
* **Repositório Alvo**: `conn2flow-ai-workspace`

---

## Objetivo

Recuperar a rastreabilidade das alterações realizadas na extensão VS Code após o encerramento formal do BATCH-036 e estabilizar o fluxo de abertura de documentos Markdown no modo "Apenas Renderizado (Preview)".

O lote deve preservar as funcionalidades já entregues, corrigir o encadeamento de abas observado ao navegar por vários documentos e impedir que a extensão feche arquivos Markdown não relacionados.

## Delta recuperado após o BATCH-036

O intervalo de commits `5b4f482..50a76c6` adicionou ou alterou, sem registro SDD correspondente:

1. refinamentos do ícone e da identidade visual da extensão;
2. navegador interativo de documentos SDD e modos de visualização;
3. seleção de escopo SDD entre Core, AI Workspace e projetos satélites;
4. modo de terminal reutilizável e detecção PowerShell/Git Bash;
5. monitoramento e criação assistida de requisições de Memory Gardening;
6. paridade de registro dos comandos entre árvore, `extension.ts` e `package.json`;
7. comandos de sincronização Core -> Projeto -> ambiente local de testes;
8. integração direta com o Custom Editor do Markdown Preview Enhanced;
9. tentativa inicial de correção do encadeamento de abas Markdown.

## Escopo aprovado

### 1. Recuperação documental

- Atualizar `CURRENT.md`, `BATCH-INDEX.md`, o registro do lote, o handoff e o checklist de validação.
- Registrar o delta pós-BATCH-036 sem reescrever a SPEC normativa.
- Manter o modo supervisionado: nenhuma operação automática de commit, push ou deploy.

### 2. Estabilização do preview Markdown

- No modo `preview`, abrir diretamente o Custom Editor `markdown-preview-enhanced` quando disponível.
- Manter apenas o preview gerenciado pela ação atual da extensão, sem acumular fontes Markdown.
- Preservar abas Markdown abertas manualmente ou pertencentes a outros fluxos.
- Garantir que o preview correspondente ao último item clicado receba foco.
- Manter os modos `code` e `both` sem regressão.
- Manter fallback para o preview nativo quando o MPE não estiver instalado ou falhar.

### 3. Validação verificável

- Criar testes automatizados para a política de seleção e fechamento de abas.
- Validar a sequência de pelo menos três documentos distintos.
- Validar preservação de uma aba Markdown não relacionada.
- Executar compilação TypeScript, empacotamento VSIX e instalação local.
- Registrar hashes ou evidências equivalentes entre fonte compilada e extensão instalada.

## Critérios de aceite

- [x] Três documentos abertos sequencialmente no modo `preview` resultam no documento mais recente renderizado e focado.
- [x] Nenhuma aba de código-fonte é acumulada pela navegação em modo `preview`.
- [x] Abas Markdown não relacionadas permanecem abertas segundo a política automatizada de abas.
- [x] Os modos `code` e `both` mantêm o comportamento declarado.
- [x] O fallback nativo não fecha documentos alheios segundo a política automatizada de abas.
- [x] Testes automatizados da política de abas passam.
- [x] `npm run compile` termina com código de saída 0.
- [x] O VSIX é empacotado e atualizado na instalação local.
- [x] Os artefatos operacionais do SDD refletem o estado real do código.

## Fora de escopo

- Reestruturação ampla da interface e hierarquia completa da árvore.
- Inclusão do navegador de backlog.
- Alterações normativas em `sdd/SPEC.md`.
- Commit, push ou publicação no Marketplace sem aprovação humana.

Essas melhorias serão revisadas após o encerramento deste lote e apresentadas em uma requisição separada para aprovação.
