# REQUISICAO HUMANA REQ-010 / BATCH-013

*   **Status**: `COMPLETED`
*   **Data de Criação**: 2026-08-18
*   **Solicitante**: Chief Architect / User
*   **Escopo**: Execução de Memory Gardening e SDD Context Archiving completo em todas as pastas (`memórias`, `validation/`, `implementation/`, `decisions/`, `human-requests/`) nos 4 repositórios privados e no workspace.

---

## 🎯 Objetivo e Diretrizes de Negócio

Garantir a máxima eficiência de contexto e prevenir a degradação de tokens aplicando o protocolo duplo de **Memory Gardening** e **SDD Context Archiving (Teto de 10 Itens Ativos + subpastas `/archive/`)** em todos os arquivos de controle que ultrapassaram os limites.

---

## 📊 Diagnóstico dos Arquivos Acima do Limite

### 1. `conn2flow`:
- `sdd/MEMORIA-ENGENHARIA-EXECUCAO.md`: **18.3 KB (210 linhas)** ➔ Podar para ~5 KB (manter 3–5 tarefas recentes).
- `sdd/validation/VALIDATION-CHECKLIST.md`: Arquivar itens antigos para `sdd/validation/archive/` (manter 10 ativos).

### 2. `lumix`:
- `sdd/validation/VALIDATION-CHECKLIST.md`: **70.0 KB (308 linhas)** ➔ Arquivar para `sdd/validation/archive/VALIDATION-CHECKLIST-ate-BATCH-XXX.md` e manter apenas os 10 mais recentes ativos.
- `sdd/MEMORIA-ENGENHARIA-EXECUCAO.md`: **12.4 KB (143 linhas)** ➔ Podar para ~5 KB (manter 3–5 tarefas recentes).
- `sdd/implementation/BATCH-INDEX.md`: Garantir teto de 10 itens ativos com referências históricas em `/archive/`.

### 3. `transformamp`:
- `sdd/validation/VALIDATION-CHECKLIST.md`: **43.7 KB (261 linhas)** ➔ Arquivar para `sdd/validation/archive/` e manter apenas 10 ativos.
- `sdd/implementation/BATCH-INDEX.md`: **14.5 KB (152 linhas)** ➔ Arquivar para `sdd/implementation/archive/`.
- `sdd/MEMORIA-ENGENHARIA-EXECUCAO.md`: **5.2 KB (54 linhas)** ➔ Lapidar para ~4.5 KB.

### 4. `conn2flow-site`:
- `sdd/decisions/DECISION-LOG.md`: **10.6 KB (165 linhas)** ➔ Arquivar decisões antigas em `sdd/decisions/archive/` (manter 10 ativas).
- `sdd/validation/VALIDATION-CHECKLIST.md`: Manter teto de 10 ativos.

### 5. `conn2flow-ai-workspace`:
- `sdd/validation/VALIDATION-CHECKLIST.md`: **15.6 KB (170 linhas)** ➔ Arquivar itens antigos para `sdd/validation/archive/` mantendo 10 ativos.

---

## 📋 Detalhamento das Tarefas de Implementação

1. **Poda de Memórias de Execução**:
   - `MEMORIA-ENGENHARIA-EXECUCAO.md` em `conn2flow`, `lumix` e `transformamp` podadas para ~4.5 KB a 5 KB (abaixo de 50 linhas), mantendo apenas as 3–5 tarefas mais recentes.
2. **Arquivamento de Checklist de Validação (`VALIDATION-CHECKLIST.md`)**:
   - Mover entradas antigas para `sdd/validation/archive/` em todos os repositórios listados, mantendo apenas os 10 itens de validação mais recentes no arquivo ativo da raiz.
3. **Arquivamento de Índice de Batches (`BATCH-INDEX.md`) e Decisões (`DECISION-LOG.md`)**:
   - Manter apenas os 10 lotes / decisões recentes no arquivo ativo, movendo registros anteriores para suas respectivas subpastas `/archive/` com referências limpas.
4. **Git Staging, Commit e Push**:
   - Executar commit e push em todos os repositórios atualizados com a mensagem:
     `chore(sdd): perform full-spectrum SDD context archiving and memory gardening across active folders`
