# REQUISICAO HUMANA REQ-030 / BATCH-032

*   **Status**: `READY-FOR-IMPLEMENTATION`
*   **Data de Criação**: 2026-08-29
*   **Solicitante**: Engenheiro Chefe / Arquiteto do Core
*   **Repositórios Alvo**: `conn2flow-ai-workspace`, `conn2flow`, `lumix`, `transformamp`, `conn2flow-site`
*   **Escopo**: 
    1. **Alinhamento e Paridade do OpenAI Codex (`.codex/`)**:
       - Atualização e criação de `AGENTS.md` canônico na raiz de todos os 5 repositórios com as 36 skills e regras de duplo agente;
       - Criação de `.codex/hooks.json` nos templates e repositórios conectando o `pre-tool-guard.ps1` e `.sh` para travas determinísticas de segurança no Codex;
       - Provisionamento de `.codex/config.toml` com diretrizes de aprovação e sandbox.
    2. **Alinhamento e Paridade do Google Antigravity (`.gemini/`)**:
       - Criação de `.gemini/hooks.json` conectando `pre-tool-guard.ps1` no evento `PreToolUse` para bloquear `git add -A` e cópias manuais;
       - Configuração do hook `Stop` em `.gemini/hooks.json` para impedir encerramento prematuro sem validação de checklist;
       - Padronização de `GEMINI.md` nos 5 repositórios.
    3. Propagação universal com `sync-all-repos.ps1 -Force` e validação com `php cli/c2f.php ai:sync`.

---

## 🎯 1. Diagnóstico & Justificativa Técnica

Com a implementação bem-sucedida das travas no Claude Code (`REQ-027`, `REQ-028` e `REQ-029`), o ecossistema precisa garantir **paridade estrita e simetria de comportamento** para os outros dois pilares da Tríade de IAs:
1. **No OpenAI Codex**:
   - `conn2flow/AGENTS.md` estava desatualizado (citava 33 skills) e os demais repositórios não possuíam o arquivo na raiz;
   - O Codex suporta a mesma especificação de Hooks que o Claude (`.codex/hooks.json`), mas ainda não estava utilizando nosso script `pre-tool-guard`.
2. **No Google Antigravity**:
   - O Antigravity suporta hooks de ciclo de vida em `.gemini/hooks.json` para eventos `PreToolUse` e `Stop`;
   - Ao adicionar o hook `Stop`, garantimos que o agente Gemini seja forçado a continuar até que o checklist do SDD esteja 100% verde.

---

## 🛠️ 2. Especificação Técnica das Alterações

### A. OpenAI Codex Kit
1. **Criar/Atualizar `AGENTS.md` na Raiz**:
   - Declarar os papéis do Agente Duplo (Arquiteto e Executor);
   - Referenciar todas as 36 skills;
   - Incluir a regra de ouro de governança (proibição de `git add -A` e cópia manual).
2. **Criar `.codex/hooks.json`**:
   - Configurar o evento `PreToolUse` para acionar `.claude/hooks/pre-tool-guard.ps1` (Windows) e `.sh` (Unix).
3. **Criar `.codex/config.toml`**:
   - Definir `approval_policy = "on-request"`.

### B. Google Antigravity Kit
1. **Criar `.gemini/hooks.json`**:
   - Configurar `PreToolUse` para a ferramenta `run_command` chamando `pre-tool-guard.ps1`;
   - Configurar o hook `Stop` garantindo conformidade de entrega.
2. **Padronizar `GEMINI.md`**:
   - Assegurar que os 5 repositórios tenham `GEMINI.md` alinhado.

### C. Atualização dos Templates & Propagação
- Atualizar os templates do Codex Kit e Gemini Kit em `templates/pt-br/` e `templates/en/`;
- Executar `scripts/sync-all-repos.ps1 -Force` para propagar em `conn2flow`, `lumix`, `transformamp`, `conn2flow-site`.

---

## 📋 Checklist de Execução

- [ ] Atualizar `AGENTS.md` com 36 skills e propagar para os 5 repositórios e templates.
- [ ] Criar `.codex/hooks.json` e `.codex/config.toml` no workspace e templates.
- [ ] Criar `.gemini/hooks.json` com `PreToolUse` e `Stop` no workspace e templates.
- [ ] Propagar universalmente com `sync-all-repos.ps1 -Force` nos 4 repositórios alvo.
- [ ] Validar conformidade de 36 skills via `php cli/c2f.php ai:sync` no `conn2flow`.
- [ ] Commitar e sincronizar no Git em todos os repositórios com `git add <caminhos-específicos>`.
