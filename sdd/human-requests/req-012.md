# REQUISICAO HUMANA REQ-012 / BATCH-015

*   **Status**: `READY-FOR-IMPLEMENTATION`
*   **Data de Criação**: 2026-08-20
*   **Solicitante**: Chief Architect / User
*   **Repositório Alvo**: `C:\Users\otavi\OneDrive\Documentos\GIT\conn2flow`
*   **Escopo**: Criação da arquitetura moderna orientada a objetos do Conn2Flow CLI na pasta raiz `cli/` com ponto de entrada executável `./c2f` e `./c2f.ps1`, mapeando as automações de `.vscode/tasks.json`.

---

## 🎯 Objetivo e Diretrizes de Negócio

Desenvolver um subsistema CLI nativo e extensível em PHP moderno (PHP 8.2+, tipagem estrita, interfaces e classes) isolado no diretório raiz `/cli/` do repositório `conn2flow`, permitindo que desenvolvedores e agentes de IA executem rotinas de recursos, banco de dados, Docker, plugins, projetos e IA de forma determinística via terminal.

---

## 🏗️ Estrutura Arquitetural Proposta (`conn2flow/cli/`)

```
conn2flow/
├── c2f                              # Wrapper executável Bash (Linux / Mac / Git Bash)
├── c2f.ps1                          # Wrapper executável PowerShell (Windows)
└── cli/
    ├── c2f.php                      # Ponto de entrada / Bootstrap do CLI
    └── src/
        ├── Contracts/
        │   ├── CommandInterface.php # Contrato: getName(), getDescription(), execute(Input, Output)
        │   └── OutputInterface.php  # Contrato de saída colorida / formatação
        ├── Console/
        │   ├── Application.php      # Roteador e despachante de comandos com auto-help
        │   ├── Input.php            # Parser de argumentos e flags (--flag, -f, args)
        │   └── Output.php           # Renderizador ANSI colorido, tabelas e caixas
        ├── Commands/
        │   ├── Ai/
        │   │   ├── SyncSkillsCommand.php       # c2f ai:sync (Sincroniza 32 skills)
        │   │   └── PruneMemoriesCommand.php    # c2f ai:prune-memories (Memory Gardening)
        │   ├── Resources/
        │   │   └── SyncResourcesCommand.php    # c2f resources:sync (atualizacao-dados-recursos.php)
        │   ├── Database/
        │   │   ├── UpdateDatabaseCommand.php   # c2f db:update (updates-manager-database.sh)
        │   │   └── TestDatabaseCommand.php     # c2f db:test (bateria de testes)
        │   ├── Module/
        │   │   └── CreateModuleCommand.php     # c2f module:create <modulo-id> (Scaffold canônico)
        │   ├── Plugin/
        │   │   ├── SyncPluginCommand.php       # c2f plugin:sync [private|public]
        │   │   └── BuildPluginCommand.php      # c2f plugin:build [private|public]
        │   ├── Project/
        │   │   ├── SyncCoreCommand.php         # c2f project:sync-core <project-id>
        │   │   └── DeployProjectCommand.php    # c2f project:deploy <project-id>
        │   └── Docker/
        │       ├── StatusDockerCommand.php     # c2f docker:status (docker ps)
        │       ├── LogsDockerCommand.php       # c2f docker:logs [--follow] (/var/log/php_errors.log)
        │       └── TruncateLogsDockerCommand.php # c2f docker:truncate-logs
        └── Support/
            └── Color.php                       # Suporte a cores ANSI e formatação terminal
```

---

## 📋 Detalhamento das Tarefas de Implementação

### 1. Criar o Núcleo do CLI em `conn2flow/cli/`:
- `declare(strict_types=1);` em todos os arquivos.
- Implementar `CommandInterface` e `OutputInterface`.
- Implementar `Application.php` com carregamento dinâmico/registro de comandos, auto-descoberta e exibição de ajuda formatada (`./c2f --help` ou `./c2f list`).
- Zero dependências externas (PHP puro e nativo).

### 2. Implementar os Comandos Baseados em `.vscode/tasks.json`:
- **`resources:sync`**: Executa a compilação de recursos de `gestor/controladores/agents/arquitetura/atualizacao-dados-recursos.php`.
- **`ai:sync`**: Invoca o script de sincronização de skills do workspace.
- **`docker:*`**: Comandos de status, logs e truncate mapeando os comandos Docker do `tasks.json`.
- **`module:create`**: Scaffold canônico baseado na skill `c2f-module-crud-scaffolding` e `modulos-grupos`.

### 3. Criar os Wrappers de Raiz:
- `conn2flow/c2f` (`#!/usr/bin/env bash` -> `php ./cli/c2f.php "$@"`) com permissão de execução.
- `conn2flow/c2f.ps1` (`php .\cli\c2f.php @args`).

### 4. Validação & Testes:
- Executar `./c2f` / `.\c2f.ps1` e verificar a listagem de comandos.
- Executar `./c2f resources:sync` e verificar a saída colorida.
- Executar testes automatizados para garantir integridade do core.

### 5. Git Staging, Commit e Push:
- No repositório `conn2flow`:
  `feat(cli): implement modern OOP CLI subsystem in /cli with c2f executables`
