# REQ-055 — Consolidação Canônica e Sincronização Global das 39 Skills nos 5 Repositórios

* **Status**: `APPROVED`
* **Data de Entrada**: 2026-09-17
* **Autor**: Humano-no-Loop & Macro-Arquiteto
* **Lote Relacionado**: `BATCH-057`
* **Topologia de Agentes**: `dupla`
* **Nível de Autonomia**: `supervisionado`
* **Repositórios Alvo**: `conn2flow-ai-workspace` (Matriz Central), `conn2flow` (Core), `conn2flow-site`, `lumix`, `transformamp`

---

## 1. Justificativa e Diagnóstico Técnico

Durante a evolução de fatias recentes no Core (`conn2flow`), melhorias críticas foram introduzidas em skills pelos agentes locais (como a blindagem de `MSYS_NO_PATHCONV=1` no rsync em `c2f-shell-and-windows-traps` e a cobertura de CSRF em `XMLHttpRequest` cru em `c2f-javascript-ajax`). No entanto, essas atualizações ficaram desfasadas na matriz central (`conn2flow-ai-workspace`) e nos demais repositórios satélites.

Além disso, a varredura comparativa identificou que as 3 skills de papéis da Tríade SDD (`c2f-architect-master`, `c2f-executor-agent`, `c2f-reviewer-agent`) estão presentes na matriz central, mas ausentes nos satélites.

Faz-se necessária uma rodada oficial de **Consolidação e Sincronização Global de Skills**:

1. **Consolidação na Matriz Central (`conn2flow-ai-workspace`)**:
   - `c2f-shell-and-windows-traps`: incorporar a **Armadilha 1 (MSYS Path Conversion em rsync sobre SSH)** exigindo `MSYS_NO_PATHCONV=1`.
   - `c2f-javascript-ajax`: incorporar a **cobertura automática de CSRF e 401 pelo `global.js`** (incluindo interceptação de `XMLHttpRequest` cru e uploads de progresso).
   - Espelhar as 39 skills em `.claude/skills/`, `.gemini/skills/` e `.codex/skills/`.

2. **Distribuição Canônica para os 5 Repositórios Privados**:
   - Propagar a totalidade das **39 skills oficiais** para:
     * `conn2flow` (`C:\Users\otavi\OneDrive\Documentos\GIT\conn2flow`)
     * `conn2flow-site` (`C:\Users\otavi\OneDrive\Documentos\GIT\conn2flow-site`)
     * `lumix` (`C:\Users\otavi\OneDrive\Documentos\GIT\lumix`)
     * `transformamp` (`C:\Users\otavi\OneDrive\Documentos\GIT\transformamp`)
     * `conn2flow-ai-workspace` (`C:\Users\otavi\OneDrive\Documentos\GIT\conn2flow-ai-workspace`)
   - Em todas as pastas de kit suportadas (`.claude/skills/`, `.gemini/skills/`, `.codex/skills/`).

3. **Auditoria de Integridade**:
   - Validar via hash criptográfico (MD5/SHA256) que 100% das 39 skills estão rigorosamente idênticas entre a matriz e os 4 satélites.

---

## 2. Escopo Técnico da Entrega

### 2.1 Matriz Central (`conn2flow-ai-workspace`):
- Atualizar `c2f-shell-and-windows-traps/SKILL.md` com a blindagem de rsync MSYS2.
- Atualizar `c2f-javascript-ajax/SKILL.md` com o contrato de CSRF em `XMLHttpRequest`.
- Garantir espelhamento nas pastas `.claude/skills/`, `.gemini/skills/` e `.codex/skills/`.

### 2.2 Sincronização nos 4 Satélites (`conn2flow`, `conn2flow-site`, `lumix`, `transformamp`):
- Copiar/sincronizar as 39 skills da matriz central para as respectivas pastas de skills de cada repositório.
- Incluir as 3 skills da Tríade SDD (`c2f-architect-master`, `c2f-executor-agent`, `c2f-reviewer-agent`).

### 2.3 Auditoria e Validação:
- Executar script de conferência de hash comprovando divergência ZERO em todas as skills.

---

## 3. Critérios de Aceite

1. `c2f-shell-and-windows-traps` contém a regra obrigatória `MSYS_NO_PATHCONV=1` para `rsync`.
2. `c2f-javascript-ajax` contém a seção de cobertura CSRF do `global.js` e `XMLHttpRequest`.
3. Todas as 39 skills presentes e sincronizadas nos 5 repositórios (`conn2flow-ai-workspace`, `conn2flow`, `conn2flow-site`, `lumix`, `transformamp`).
4. Script de auditoria reporta zero divergências de hash entre matriz e satélites.
5. Gate SDD limpo e recibo emitido em `completions/BATCH-057-executor-receipt.json`.
