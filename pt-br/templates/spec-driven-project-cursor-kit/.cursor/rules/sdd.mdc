---
description: "Aplicar a governança Spec-Driven Development ao ler ou editar artefatos em sdd/. Invoque explicitamente as skills obrigatórias por marco de fluxo."
globs: ["sdd/**/*"]
alwaysApply: false
---

# Governança SDD

1. Leia `sdd/README.md`, `sdd/process/00-START-HERE.md`, `sdd/process/01-WORKFLOW.md`, `sdd/implementation/BATCH-INDEX.md`, o batch ativo, `sdd/validation/VALIDATION-CHECKLIST.md` e `sdd/decisions/DECISION-LOG.md`.
2. Se a tarefa apontar para `sdd/human-requests/`, leia `CURRENT.md` e a requisição ativa primeiro; classifique-a no artefato SDD correto.
3. Invoque explicitamente as Skills Obrigatórias por Marco de Fluxo (`sdd-workflow`, `project-validation`, Core Skills `c2f-*`, `sdd-memory-gardening`).
4. Como Executor, escreva apenas no escopo operacional aprovado: código, batches e validação.
5. Trate `sdd/SPEC.md`, especificações numeradas e `DECISION-LOG.md` como somente leitura, salvo autorização humana explícita.
6. Nunca altere a memória de Chefia sem ordem humana. Mantenha a memória de Execução abaixo dos limites de `sdd/process/MEMORY-GARDENING-GUIDELINES.md`.
7. Preserve alterações locais alheias, valide cedo e registre evidências no batch e no checklist.
8. Trate `sdd/backlog/` como incubadora de leitura exclusiva do Usuário/Arquiteto. Nunca implemente um item dali; exija promoção humana para `sdd/human-requests/`, `CURRENT.md` e batch executável.


## 📋 Protocolo de Transparência & Checklist Vivo (Live Todo List)

- Ao iniciar qualquer requisição ou lote, renderize imediatamente a lista completa de tarefas (`Todo List`) com caixas de seleção `[ ]`.
- A cada término de etapa/comando relevante, atualize e re-exiba a lista marcando `[x]` nas etapas concluídas e destacando a etapa atual (`⏳ [EM ANDAMENTO]`).
- Nunca execute sequências longas de comandos sem atualizar o status visual para o usuário.

## 🛡️ Modos de Autonomia de IA & Trava de Deploy

- **Modo SUPERVISIONADO (Padrão Mandatório)**:
  * O agente implementa código e roda testes, mas **NÃO realiza commit ou deploy automático**.
  * O desenvolvedor revisa e aprova as mudanças no chat/IDE.

- **Modo AUTÔNOMO (Apenas quando explicitado na requisição / usuário)**:
  * Permitido quando a requisição contiver `modo: autonomo` ou o usuário autorizar expressamente.
  * O agente pode: criar branch/worktree (`feat/req-XXX`), codificar, compilar (`c2f resources:sync`), rodar testes (`c2f db:test`), commitar e executar **DEPLOY EXCLUSIVAMENTE EM AMBIENTE DE TESTE LOCAL** (`c2f manager:update-all` ou Docker local).
  * ⛔ **REGRA INVIOLÁVEL DE SEGURANÇA: NUNCA REALIZAR DEPLOY AUTOMÁTICO NO AMBIENTE DE PRODUÇÃO OU SERVIDORES REMOTOS.**
