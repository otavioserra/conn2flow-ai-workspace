# Human Requests Archive

This folder stores older human requests removed from the active `../` area when there are more than 10 current or recent items.

Keep archived requests reachable through direct links from the main files, using concise Markdown summary tables with 1 row per archived item.
